# Markdown Math

**Notice:** This extension is bundled with BillAI. It can be disabled but not uninstalled.

Adds math rendering using [KaTeX](https://katex.org) to BillAI's built-in markdown preview and markdown cells in notebooks.
